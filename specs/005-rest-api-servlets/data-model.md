# Data Model for REST API Servlets

This feature does not introduce any new database entities or modify the existing data model. It provides an API layer to interact with the following existing entities:

-   **Conversation**
-   **Message**
-   **Tenant**
-   **User**

All data access is performed through existing services, and no direct database schema changes are required.
