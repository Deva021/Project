# API Contracts for Frontend UI Components

This feature primarily consumes existing backend API endpoints defined in the `006-frontend-core` specification. Detailed API contracts can be found in:

*   `../006-frontend-core/contracts/api.yaml`

No new API endpoints or modifications to existing ones are introduced as part of this Frontend UI implementation.
